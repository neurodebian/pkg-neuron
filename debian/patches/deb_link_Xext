--- a/configure.in
+++ b/configure.in
@@ -311,7 +311,7 @@ elif test "$enable_carbon" = "yes" ; the
 else
 	AC_PATH_XTRA   dnl Find X11.
 	if test "$no_x" = "" ; then
-		X_LIBS="$X_LIBS -lX11"
+		X_LIBS="$X_LIBS -lX11 -lXext"
 	fi
 fi
 AC_SUBST(enable_carbon)
